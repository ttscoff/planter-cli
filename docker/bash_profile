#!/bin/bash
export GLI_DEBUG=true
export EDITOR="/usr/bin/vim"
alias b="bundle exec bin/plant"
alias quit="exit"

shopt -s nocaseglob
shopt -s histappend
shopt -s histreedit
shopt -s histverify
shopt -s cmdhist

cd /planter
bundle install
gem install pkg/*.gem
